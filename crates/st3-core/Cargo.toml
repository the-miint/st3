[package]
name = "st3-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "SourceTracker3 core: preprocessing, the Gibbs estimator, and collation. No FFI, no Arrow, no I/O."

[dependencies]
rand = { workspace = true }
rand_xoshiro = { workspace = true }
rand_distr = { workspace = true }
rayon = { workspace = true }

[dev-dependencies]
criterion.workspace = true
# Parses criterion's estimates.json in the opt-in wall-clock perf guard
# (`tests/perf_guard.rs`, `#[ignore]`d).
serde_json.workspace = true

[[bench]]
name = "kernels"
harness = false
